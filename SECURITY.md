# 安全说明

本项目不是京东云、LibWrt 或 iStoreOS 的官方固件。使用者负责确认设备型号、分区布局、下载完整性和恢复路径；任何实际刷写都应在本地、由设备所有者明确执行。

## 凭据与首次登录

固件不预置任何默认账户、密码或其他凭据，也不包含代理节点、证书或云服务密钥。首次从可信 LAN 登录后，必须设置强度足够且唯一的 root 密码；在设置 root 密码前，不应把管理接口暴露给不可信网络。

不要在 Issue、构建日志、Release 说明或截图中公开路由器密码、配置备份、私钥、令牌、MAC、序列号或设备特有分区内容。

## 管理接口边界

- LuCI 和 SSH 应只向可信管理网络开放；不要自行添加 WAN 入站规则。
- iStore 变体包含 `luci-app-ttyd`，但 ttyd 不新增任何 WAN 防火墙开放规则。它只计划供 LAN 侧通过 LuCI 使用，WAN 不可访问仍须在真机验收中验证。
- 固件不会自动启用 PassWall2、MosDNS、AdGuard Home、Docker、Tailscale 或 SQM，避免未配置服务抢占端口或产生意外暴露。
- 固件不会改变 LAN 默认地址，不会自动挂载 `storage` 或启用 swap。

## 构建与数据边界

GitHub Actions 只克隆公开源码、读取仓库配置、编译并发布产物。构建器不会连接或自动刷写路由器，也不会读取、上传或传出路由器数据。不要把设备备份或私人配置提交到仓库或上传为 Actions Artifact。

发布产物必须附带 `SHA256SUMS`。刷写前应从同一个 Release 下载校验清单并验证 SHA-256；校验不符时停止使用。iStore 中单个第三方应用不保证兼容 LibWrt 25.12 或本设备，安装前应单独评估来源、维护状态、权限和网络暴露。

## beta 验收门槛

iStore `0.1.0-beta.1` 是 Prerelease，不能标记为 Latest。云编译通过不代表安全边界已经在设备上成立；必须在 RE-SS-01 真机上验证 root 密码、LuCI、SSH、ttyd LAN/WAN 隔离、默认服务状态、重启和断电重启。在这些物理检查完成前，不得合并到 `main`，Argon `1.0.0` 仍是稳定推荐。

## 报告问题

公开且不含敏感信息的安全问题可在仓库 Issue 中提交。若问题涉及可利用细节、凭据或设备专有数据，请优先使用 GitHub 的私密安全报告功能；不要先公开完整利用步骤或敏感附件。
